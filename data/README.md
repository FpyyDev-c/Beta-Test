Estes arquivos (`servers.json`, `profiles.json`) são o "banco de dados" do FpyyCord.
Eles são lidos e gravados automaticamente pelo site através da API do GitHub
(veja `js/gh-db.js`). Você não precisa editá-los manualmente — mas pode, se
quiser corrigir algo à mão (é só um JSON simples).
